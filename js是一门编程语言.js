/**
 * 首先js是一门高级的编程语言
 *
 * 从编程语言角度来看 可以划分为三个阶段
 *
 * - 机器语言: 10101001010 一些机器指令
 * - 汇编语言: bx 一些汇编指令
 * - 高级语言: go dart typeScript swift rust
 *
 * 而高级语言中也可以分为两大类:
 * - 编译性语言 C C++这一类语言就是编译过后变成可执行文件 随后计算机进行读取执行
 * - 解释性语言 js python 机器边读取源代码边进行解释 随后计算机进行执行
 *
 * 但是计算机它本身是不认识这些高级语言 所以我们的代码最终还是需要被转换成机器指令
 */
