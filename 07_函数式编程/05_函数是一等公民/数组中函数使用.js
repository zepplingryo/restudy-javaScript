var arr = [1, 2, 3, 4, 5];

// arr
//   .filter((ele) => (ele % 2 === 0 ? true : false))
//   .forEach((r) => console.log(r));

var newArr = arr.filter(function (item) {
  return item % 2 === 0;
});

console.log(newArr);

// map

// forEach

// reduce

// find
